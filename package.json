{
  "name": "budget-manager-fullstack",
  "version": "1.0.0",
  "description": "Complete budget management web application",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd . && nodemon server.js",
    "client": "cd client && npm run dev",
    "build": "cd client && npm ci && npm run build",
    "start": "node server.js",
    "install-all": "npm install && cd client && npm install",
    "deploy-setup": "echo 'Setting up deployment environment...'"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "jest": "^29.6.1",
    "nodemon": "^3.0.1"
  }
}
